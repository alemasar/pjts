<config>
  {
    "tag": "demo-first-tag"
  }
</config>
<template>
  <h1 class="hola">{{ data:hello.message:"HELLO WORLD" }} FROM <span cat-for="t in data:test:["HELLO", "BYE"]">{{ t }}</span> WEB COMPONENT</h1>
</template>
